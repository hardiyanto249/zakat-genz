server {
    listen 80;
    server_name kuy-zakat.lazmitra.my.id;

    root /var/www/kuy-zakat;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    # SSL configuration (Enable after setting up Let's Encrypt)
    # listen 443 ssl;
    # ssl_certificate /etc/letsencrypt/live/kuy-zakat.lazmitra.my.id/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/kuy-zakat.lazmitra.my.id/privkey.pem;
}
